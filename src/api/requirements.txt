# FastAPI 관련 의존성 (필수)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0

# CORS 및 정적 파일 처리
python-multipart>=0.0.6

# ===== 기본 의존성 =====
python-dotenv==1.1.1
tqdm>=4.65.0
psutil>=5.9.0

# ===== LangChain 코어 (호환 버전) =====
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community==0.3.27
langchain-text-splitters>=0.3.0

# ===== LangChain AI 제공자들 =====
langchain-ollama==0.3.6
langchain-huggingface==0.3.1

# ===== AI API 클라이언트 =====
ollama>=0.1.0

# ===== 벡터 데이터베이스 =====
elasticsearch==8.11.0

# ===== PDF 및 문서 처리 =====
pypdf>=3.0.0
python-docx>=0.8.11

# ===== 임베딩 및 ML (C++ 빌드 문제 해결) =====
# sentence-transformers와 호환되는 안정 버전 사용
sentence-transformers>=2.2.2,<3.0.0

# BGE 임베딩 모델 - 안정 버전으로 변경 (zlib-state 의존성 문제 회피)
# FlagEmbedding>=1.2.0  # C++ 빌드 도구 필요로 인해 주석 처리
# 대신 sentence-transformers로 BGE 모델 직접 사용

# Hugging Face 모델 허브 접근용
huggingface-hub>=0.16.0
transformers>=4.35.0
accelerate>=0.20.0

# 수치 계산 라이브러리
numpy>=1.21.0
torch>=2.0.0

# ===== 모니터링 =====
langsmith>=0.1.0
langfuse>=2.0.0

# 클라이언트용 HTTP 라이브러리
aiohttp>=3.8.0
requests>=2.31.0